import styled from 'styled-components';

const ManageContent = styled.div`
	width: 100%;
    padding: 0 20px;

    h1 {
        margin-bottom: 20px;
		color: var(--Gray05);
		font-size: var(--F06);
    }
`;

export default ManageContent;